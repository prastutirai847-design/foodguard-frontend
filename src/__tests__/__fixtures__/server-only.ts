// Test stub for the `server-only` marker package. In the Vitest node
// environment there is no client/server component distinction, so importing
// server-only should be a no-op instead of throwing (as the real package does).
